//! persona-wire Adapter for mini-app SoT (scheme `mini-app://`).
//!
//! P3b roadmap deliverable: external adapter crate split out from
//! `persona-wire-core`. Step 1 = empty skeleton (this file). Step 2 will
//! physically move `MiniAppAdapter` + URI parse + tests from core into here.
