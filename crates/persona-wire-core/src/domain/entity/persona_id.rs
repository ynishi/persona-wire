//! `PersonaId` — owner identity reference.
//!
//! Value object. The persona's full identity SoT lives in the external
//! `persona-pack` crate; `ContextWiring` only carries the id by reference.
//!
//! Step B: skeleton. Field / invariant / behaviour land in Step C.
