//! `Wiring` Entity — 1 slot binding (surrogate key pattern).
//!
//! Owns `Source` and `Projection` directly. Has lifecycle (create / update /
//! delete) and mutable attributes (source URI / projection template).
//!
//! Step B: skeleton. Rich impl lands in Step C.
