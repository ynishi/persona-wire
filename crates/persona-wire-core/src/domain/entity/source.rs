//! `Source` Entity — SoT location (URI) that a `Wiring` points at.
//!
//! Persona-owned: alias varies per persona (`?alias=for_dolly` vs
//! `?alias=for_shi`). Owned by `Wiring`.
//!
//! Step B: skeleton. Rich impl lands in Step C.
