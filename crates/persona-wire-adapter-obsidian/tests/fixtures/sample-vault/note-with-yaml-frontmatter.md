---
title: Note with YAML Frontmatter
tags: [example, frontmatter]
aliases: [Aliased Note Title]
custom_field: generic-test-value
---

# Note with YAML Frontmatter

This is a sample note used by `wire-adapter-obsidian` integration tests.
It references [[Other Note]] and also [[Linked Note|表示テキスト]] using alias form.

```code
[[fake link inside code fence — should NOT be extracted]]
```

End of note.
