{
  "crate": "persona-wire-adapter-notion",
  "version": "0.12.0",
  "items": [
    {
      "path": "persona_wire_adapter_notion::API_BASE",
      "kind": "constant",
      "docs": "Notion API base URL."
    },
    {
      "path": "persona_wire_adapter_notion::DEFAULT_LIMIT",
      "kind": "constant",
      "docs": "Default `items` cap when `?limit=` is absent from the URI."
    },
    {
      "path": "persona_wire_adapter_notion::FETCH_TIMEOUT",
      "kind": "constant",
      "docs": "Per-request HTTP timeout (connect + body), matching\n`persona-wire-adapter-todoist::FETCH_TIMEOUT`."
    },
    {
      "path": "persona_wire_adapter_notion::MAX_LIMIT",
      "kind": "constant",
      "docs": "Maximum allowed `?limit=` value (Notion API's own `page_size` ceiling)."
    },
    {
      "path": "persona_wire_adapter_notion::NOTION_VERSION",
      "kind": "constant",
      "docs": "Notion API version pinned by this adapter (sent via the `Notion-Version`\nheader on every request). As of this version, `archived` was renamed to\n`in_trash` on page/block objects, and search's `filter.value` enum uses\n`data_source` (not the legacy `database`)."
    },
    {
      "path": "persona_wire_adapter_notion::NotionAdapter",
      "kind": "struct",
      "docs": "persona-wire Adapter for Notion (`notion://` scheme)."
    },
    {
      "path": "persona_wire_adapter_notion::TEXT_MAX_CHARS",
      "kind": "constant",
      "docs": "Max block/title text length in `char`s before truncation (context size\nguard)."
    }
  ]
}
