# persona-wire-core::application::doctor::registry

Probe registry — default 全埋め込み。

新 Probe 追加時はここに 1 行 push する。 wire_doctor 本体 / lib.rs MCP 経路は触らない。

## Functions

- `default` — default 全埋め込み。 順序は最終 Markdown 出力順 (Graph 軸 → Workflow 軸)。

