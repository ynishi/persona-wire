# persona-wire concept (P0 design doc)

Status: P0 land (initial concept consolidation)
Date: 2026-06-14
Authors: shi (drafting) + ytk (design direction)

## 1. Origin

Three issue thread converged into this concept:

| Issue | Origin | Layer contribution |
|---|---|---|
| `543a0f01` | tomo (`personal`, latest 2026-06-14) | **Concept base** — メタ Context Routing + Edge/Node Graph + Lifecycle + Knowledge SoT navigator |
| `348f48eb` | shi (`persona-work`, 2026-06-14 morning) | **Application surface** — 7 pnet_* primitive (init / update / close / doctor / query / net / spawn / retire) |
| `d0df865b` | shi (`persona-work`, 2026-06-14 morning) | **Storage / engine** — 5 core primitive (Graph / CRUD / Compute / Prompt / AutoVersion) |

Naming note: "persona-network" was rejected because the "network" label leaks into UI/marketing scope confusion. "persona-wire" picked as working title to keep the concept neutral (synapse / graph / wire 等の仮称、 後で確定可)。

## 2. Core value proposition

**ProjectionAsPrompt** — arbitrary graph query → template binding → LLM-consumable Prompt (or Markdown / JSON / ASCII).

The graph is open-vocabulary (Node type / Edge type are registered, not hardcoded), so persona-wire serves as a **workflow engine / graph engine** that can host:
- Persona relation map (`routes_to` friend graph) seed
- Workflow definitions (step nodes + `transitions_to` edges + Constraint edges as guards)
- SoT cross-reference (`cites` / `derives_from`) across outline / mini-app / persona-pack / persona-journal / mia_presents
- Lifecycle monitoring (`triggers_review_of` edges + Daemon scan)
- User-defined custom domains (registered at runtime)

## 3. Architecture (DDD + Hexagonal, BP-aligned v4)

```
┌─[1] Surface              MCP Tool / CLI subcommand
├─[2] Application          Use Cases + SpecRegistry + ProjectionRegistry
├─[3] Domain Core          Graph / CRUD / Compute / Constraint / AutoVersion / Specification
├─[4] Infrastructure       SqliteStorage + RenderingAdapter
├─[5] Daemon               triggers / constraint / workflow / lifecycle scan
├─[6] Adapter (SoT)        outline / mia_presents / persona-pack / mini-app / journal + plugin slot
└─[7] Output channel       mailbox / file / Web (V2) / Tool return
```

### 3.1 Why Specification (BP研究結果)

Per `Specification pattern` (Evans / Fowler / Greg Young):
- First-class composable predicate object
- Supports `and` / `or` / `not` runtime composition
- Domain-neutral (meta-layer, not domain-specific)
- Stored in `SpecRegistry` (Application layer) as named entries

This satisfies the "arbitrary query, dynamic + fixed both" requirement directly without independent invention.

### 3.2 NamedProjection (固定軸)

CQRS Read Model pattern: registered named queries with template binding and target form (Prompt / Markdown / JSON / ASCII). Lives in `ProjectionRegistry` (Application layer).

### 3.3 Rendering (Infrastructure)

Rendering adapter consumes `(Specification, template, target_form)` and produces output text. Template engine selection: see §6 Open question.

## 4. 8 残課題 from `543a0f01` — 4 確定 (P0 scope)

### 4.1 Edge type vocabulary (確定: seed 6+3 = 9 種)

Open vocabulary, but P0 seed:

| kind | severity 可 | 用途 |
|---|---|---|
| `triggers_review_of` | hard / soft / advisory | Lifecycle daemon の中心 edge |
| `cites` | — | SoT cross-reference |
| `derives_from` | — | Narrative chain (handoff → chapter) |
| `routes_to` | — | RelationMap (友達ねっと / persona routing) |
| `instance_of` | — | persona-pack field ↔ 実 instance |
| `versions_of` | — | AutoVersion chain link |
| `constraint` (internal) | — | edge-as-constraint、 `metadata.expr` で DSL/Lua eval |
| `transitions_to` (internal) | — | Workflow step transition、 `metadata.guard` で guard 条件 |
| `projects_into` (internal) | — | projection_def → 対象 node group link |

### 4.2 Node type vocabulary (確定: seed 5+4 = 9 種)

| type | 用途 |
|---|---|
| `outline_node` | outline-mcp の node 1:1 |
| `mia_artifact` | mia_presents/ 配下 artifact |
| `pp_field` | persona-pack `[extra.*]` field |
| `ma_row` | mini-app 1 row |
| `pj_chapter` | persona-journal chapter |
| `persona` (internal) | persona 1 体 = wire 上の hub node |
| `channel` (internal) | mailbox / mail channel |
| `workflow_def` (internal) | Workflow 定義 |
| `projection_def` (internal) | NamedProjection 定義 |

### 4.3 depth_limit (確定: 1 hop default)

V2 で `max_depth` 可変化 carry。 P0-P5 は 1 hop で start (= multi-hop は外部 LLM が必要回数 query を回す前提)。

### 4.4 Dump format (確定: JSON-LD primary + frontmatter export)

- **Primary persistence form**: SQLite (`nodes` / `edges` / `versions` table)
- **Portable dump**: JSON-LD (Graph 構造を保持、 外部 graph tool で読み込み可)
- **Export only**: Markdown frontmatter (Obsidian / 静的サイト用)

## 5. 残 4 課題 (P0 scope 外、 carry)

| # | 課題 | 取り扱い |
|---|---|---|
| 1 | ともの居場所 | persona-wire MCP 内 sub layer (= 単独 MCP ではない)。 ともちゃん自身は 1 persona として配備 |
| 2 | triggers daemon 配備先 | persona-wire MCP 内 background task (= 別 process 不要) |
| 3 | 5 SoT importer 設計 | P1-P3 で 順次 (P1 = mini-app / persona-pack / outline / P3 = journal / mia_presents) |
| 4 | Daily report 統合 view | mailbox kind=info で broadcast または 特定 persona 宛 |

## 6. Open question (P0 解決推奨)

**Template DSL 選定** — `Rendering` adapter で使う template engine の候補:

| 候補 | PRO | CON | Rating |
|---|---|---|---|
| (a) **Lua snippet** | mlua-mcp-server / mlua-batteries 既存 reuse / DSL 学習コストゼロ / sandbox 軽量 | mlua-mcp-server crate 依存追加 (workspace に組み込む path も検討) | **9/10** [推定: 既存資産流用 + sandbox 性] |
| (b) Tera / Jinja 系 | macro 強い / 学習コスト低い (Django / Jinja2 経験者多い) | 外部 dep 追加、 動的 LL の表現力では Lua より落ちる | 7/10 |
| (c) 自前 minimal lang (Mustache レベル) | dep ゼロ / boundary 明確 | 機能不足が早期に露呈、 後で書き直し risk | 5/10 |

推奨: **(a) Lua snippet**、 Rating 差 +2 以上で AI 決定可能領分。 ただし「mlua-mcp-server crate 依存追加」 を workspace に組み込むか別 path で wire するかは User judgment 領分 (architecture 軸の choice)。

## 7. Phase plan

| Phase | Scope | 担当 layer |
|---|---|---|
| **P0** | 本 doc land + Template DSL 確定 + LICENSE land + git init | doc only |
| **P1** | Storage primitive 実装 (schema 投入) + Domain Core 6 primitive 実装 + pnet_init / pnet_close use case | Layer 2-4 着地 |
| **P2** | Adapter (mini-app / persona-pack / outline) + pnet_doctor / pnet_query + 動的 Specification 経路 verify | Layer 6 (3 SoT) |
| **P3** | Daemon (triggers + Constraint + Workflow scheduler + Lifecycle scan) + Output channel (mailbox kind=info) + Adapter (journal + mia_presents) | Layer 5 + 6 (残 2 SoT) |
| **P4** | RelationMap seed (`wire_friend_*` Tool) + pnet_spawn / pnet_retire / pnet_net | Layer 1 拡充 |
| **P5** | WorkflowEngine seed (`wire_workflow_*` Tool) + pnet_update (cross-ref 自動維持) | Layer 1 拡充 |
| **P6+** | Web view / multi-hop traversal / 高級 Compute (centrality 等) / user-defined plugin slot 公開 | V2 |

## 8. Sibling / 関連 issue

- `543a0f01` (tomo, personal) — concept origin
- `348f48eb` (shi, persona-work) — 7 pnet_* primitive surface origin
- `d0df865b` (shi, persona-work) — 5 core primitive (mini-app 拡張 MVP 案、 v1 で破棄、 内部 SQLite path で着地)
- `0e4c64aa` (shi, persona-work) — mailbox metrics aggregator (Daily report 系の前駆)
- `a95b868b` (shi, persona-work) — Self-Update sub-layer (drift sensor、 P3 Daemon と sibling)
- `863c54ed` (outline rule) / `c3a0ed4a` (mini-app Aggregator) / `e4d06ff8` (handoff journal-mcp) / `cb1b0ebc` (/wake DRY) — sub-layer 個別改修、 並走可

## 9. References

- Specification pattern (Evans / Fowler / Greg Young): https://en.wikipedia.org/wiki/Specification_pattern
- CQRS Pattern (Microsoft Azure docs): https://learn.microsoft.com/en-us/azure/architecture/patterns/cqrs
- Hexagonal Architecture (Garrido Paz): https://jmgarridopaz.github.io/content/hexagonalarchitecture.html
- Materialized View pattern (Microsoft Azure docs): https://learn.microsoft.com/en-us/azure/architecture/patterns/materialized-view
- Ardalis.Specification (.NET OSS reference impl): https://github.com/ardalis/Specification

## 10. License

Dual: MIT OR Apache-2.0 (workspace-wide).
